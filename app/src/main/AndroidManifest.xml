<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.internalpractical">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InternalPractical">
        <activity
            android:name=".Q1_Display"
            android:label="Q1 Display"
            android:parentActivityName=".Q1_Login" />
        <activity
            android:name=".Q1_Login"
            android:label="Q1 Login"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Q2"
            android:label="Q2"
            android:parentActivityName=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Q1" />
        <activity
            android:name=".MainActivity"
            android:label="Dashboard">

        </activity>
    </application>

</manifest>